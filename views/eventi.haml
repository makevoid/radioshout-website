- link = "/events/"+id+"-"+name_url

.event.article
  %h2
    %a{ href: link }
      %span.date
        = haml.format_date(published_at)
        = "-"
      = title
  .text
    != haml.article_preview(text)
