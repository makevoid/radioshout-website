guard 'livereload' do
  watch(%r{public/views/.+\.(haml)})
  watch(%r{public/sass/.+\.(sass)})
  watch(%r{public/coffee/.+\.(coffee)})
  watch(%r{public/routes\.json})
  # watch(%r{index\.html})
end

guard 'sass', input: 'public/sass', output: 'public/css'